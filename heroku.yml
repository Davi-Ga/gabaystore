setup:
  addons:
  - plan: heroku-postgresql
build:
  docker:
    web: Dockerfile
run:
  web: python manage.py makemigrations && python manage.py migrate && gunicorn app.wsgi:application -b 0.0.0.0:$PORT --log-file -